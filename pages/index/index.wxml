<!-- index.wxml -->
<view class="page">
  <!-- 顶部轮播图 -->
  <view>
    <swiper class="swiper-container" indicator-dots="{{true}}" autoplay="{{true}}" circular="{{true}}">
      <swiper-item class="swiper-item" wx:for="{{slides}}" wx:key="id">
        <navigator class="navigator" hover-class="none" url="/pages/list/index?id=1">
          <image class="item-image" src="{{item.image}}" lazy-load="{{true}}" mode="aspectFill" />
        </navigator>
      </swiper-item>
    </swiper>
  </view>
  <view class="menu">
    <navigator class="menu-item" wx:for="{{menu}}" wx:key="id" hover-class="none" url="/pages/list/index?id={{item.id}}">
      <image class="item-image" src="{{item.icon}}" lazy-load="{{true}}" mode="aspectFit" />
      <text class="item-text">{{item.name}}</text>
    </navigator>
  </view>
</view>
